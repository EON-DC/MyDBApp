server:
  port: 8085

spring:
  jpa:
    hibernate:
      ddl-auto: create
      dialect: org.hibernate.dialect.PostgreSQL10Dialect
      format_sql: true
    show-sql: true


  datasource:
    url: jdbc:postgresql://localhost:5432/test
    username: gwang
    password: password


